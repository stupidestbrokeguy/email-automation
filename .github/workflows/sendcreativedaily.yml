name: Creative Daily YouTube Upload

on:
  schedule:
    # Runs every day at 6:00 AM UTC (10:00 AM Dubai time)
    - cron: '0 6 * * *'
  # Manual trigger from GitHub UI
  workflow_dispatch:
    inputs:
      target_date:
        description: 'Target date (YYYY-MM-DD) - leave empty for today'
        required: false
        type: string
        default: ''
      no_youtube:
        description: 'Skip YouTube upload'
        required: false
        type: boolean
        default: false
      duration:
        description: 'Video duration in seconds (17-21 recommended)'
        required: false
        type: number
        default: 18

jobs:
  process-and-upload:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      actions: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
        lfs: true
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install PyMuPDF moviepy google-api-python-client google-auth-oauthlib google-auth-httplib2 pillow numpy
    
    - name: Install FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg
    
    - name: Decode and create client_secrets.json
      run: |
        if [ -z "${{ secrets.CLIENT_SECRETS_JSON }}" ]; then
          echo "❌ ERROR: CLIENT_SECRETS_JSON secret is missing!"
          exit 1
        fi
        echo "${{ secrets.CLIENT_SECRETS_JSON }}" | base64 -d > client_secrets.json
        echo "✅ client_secrets.json created"
    
    - name: Decode and create token.pickle
      run: |
        if [ -n "${{ secrets.YOUTUBE_TOKEN }}" ]; then
          echo "${{ secrets.YOUTUBE_TOKEN }}" | base64 -d > token.pickle
          echo "✅ token.pickle restored"
        else
          echo "⚠️ No token.pickle secret found - will need manual auth"
        fi
    
    - name: Setup PDF file
      run: |
        echo "🔍 Setting up PDF file..."
        
        if [ -f "your_document.pdf" ]; then
          echo "✅ Found PDF: your_document.pdf"
          ls -la your_document.pdf
          
          # Check PDF page count
          python -c "
import fitz
doc = fitz.open('your_document.pdf')
print(f'📄 PDF has {len(doc)} pages')
if len(doc) < 30:
    print('⚠️ Warning: PDF has fewer than 30 pages')
"
        else
          echo "❌ your_document.pdf not found!"
          exit 1
        fi
    
    - name: Run Creative Daily script
      id: creative_daily
      run: |
        # Get date from input or use today
        if [ -n "${{ github.event.inputs.target_date }}" ]; then
          TARGET_DATE="${{ github.event.inputs.target_date }}"
          echo "🎯 Using custom date: $TARGET_DATE"
        else
          TARGET_DATE=$(date +%Y-%m-%d)
          echo "📅 Using today's date: $TARGET_DATE"
        fi
        
        # Build command
        COMMAND="python check.py $TARGET_DATE"
        
        # Add no-youtube flag
        if [ "${{ github.event.inputs.no_youtube }}" = "true" ]; then
          COMMAND="$COMMAND --no-youtube"
          echo "📹 YouTube upload disabled"
        fi
        
        # Add duration
        DURATION="${{ github.event.inputs.duration }}"
        if [ -n "$DURATION" ] && [ "$DURATION" != "null" ]; then
          COMMAND="$COMMAND --duration=$DURATION"
          echo "⏱️ Using duration: ${DURATION}s"
        fi
        
        echo "========================================"
        echo "🚀 Executing: $COMMAND"
        echo "========================================"
        
        # Execute the command
        $COMMAND
        
        # Save video info if created
        if [ -d "extracted_date_pages" ]; then
          VIDEO_FILE=$(ls extracted_date_pages/*.mp4 2>/dev/null | head -1)
          if [ -n "$VIDEO_FILE" ]; then
            echo "video_file=$VIDEO_FILE" >> $GITHUB_OUTPUT
            echo "✅ Video created: $VIDEO_FILE"
          fi
        fi
      env:
        PYTHONUNBUFFERED: 1
    
    - name: Upload video as artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: creative-daily-video-${{ github.run_id }}
        path: extracted_date_pages/*.mp4
        retention-days: 30
    
    - name: Upload images as artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: extracted-images-${{ github.run_id }}
        path: extracted_date_pages/*.png
        retention-days: 30
    
    - name: Upload all outputs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: creative-daily-outputs-${{ github.run_id }}
        path: |
          extracted_date_pages/
          *.log
        retention-days: 14
    
    - name: Summary
      if: always()
      run: |
        echo "========================================"
        echo "📊 CREATIVE DAILY SUMMARY"
        echo "========================================"
        echo "Run ID: ${{ github.run_id }}"
        echo "Status: ${{ job.status }}"
        echo ""
        
        if [ -d "extracted_date_pages" ]; then
          echo "📁 extracted_daname: Creative Daily YouTube Upload

on:
  schedule:
    # Runs every day at 6:00 AM UTC (10:00 AM Dubai time)
    - cron: '0 6 * * *'
  # Manual trigger from GitHub UI
  workflow_dispatch:
    inputs:
      target_date:
        description: 'Target date (YYYY-MM-DD) - leave empty for today'
        required: false
        type: string
        default: ''
      no_youtube:
        description: 'Skip YouTube upload'
        required: false
        type: boolean
        default: false
      duration:
        description: 'Video duration in seconds (17-21 recommended)'
        required: false
        type: number
        default: 18

jobs:
  process-and-upload:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      actions: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
        lfs: true
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install PyMuPDF moviepy google-api-python-client google-auth-oauthlib google-auth-httplib2 pillow numpy
    
    - name: Install FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg
    
    - name: Decode and create client_secrets.json
      run: |
        if [ -z "${{ secrets.CLIENT_SECRETS_JSON }}" ]; then
          echo "❌ ERROR: CLIENT_SECRETS_JSON secret is missing!"
          exit 1
        fi
        echo "${{ secrets.CLIENT_SECRETS_JSON }}" | base64 -d > client_secrets.json
        echo "✅ client_secrets.json created"
    
    - name: Decode and create token.pickle
      run: |
        if [ -n "${{ secrets.YOUTUBE_TOKEN }}" ]; then
          echo "${{ secrets.YOUTUBE_TOKEN }}" | base64 -d > token.pickle
          echo "✅ token.pickle restored"
        else
          echo "⚠️ No token.pickle secret found - will need manual auth"
        fi
    
    - name: Setup PDF file
      run: |
        echo "🔍 Setting up PDF file..."
        
        if [ -f "your_document.pdf" ]; then
          echo "✅ Found PDF: your_document.pdf"
          ls -la your_document.pdf
          
          # Check PDF page count
          python -c "
import fitz
doc = fitz.open('your_document.pdf')
print(f'📄 PDF has {len(doc)} pages')
if len(doc) < 30:
    print('⚠️ Warning: PDF has fewer than 30 pages')
"
        else
          echo "❌ your_document.pdf not found!"
          exit 1
        fi
    
    - name: Run Creative Daily script
      id: creative_daily
      run: |
        # Get date from input or use today
        if [ -n "${{ github.event.inputs.target_date }}" ]; then
          TARGET_DATE="${{ github.event.inputs.target_date }}"
          echo "🎯 Using custom date: $TARGET_DATE"
        else
          TARGET_DATE=$(date +%Y-%m-%d)
          echo "📅 Using today's date: $TARGET_DATE"
        fi
        
        # Build command
        COMMAND="python check.py $TARGET_DATE"
        
        # Add no-youtube flag
        if [ "${{ github.event.inputs.no_youtube }}" = "true" ]; then
          COMMAND="$COMMAND --no-youtube"
          echo "📹 YouTube upload disabled"
        fi
        
        # Add duration
        DURATION="${{ github.event.inputs.duration }}"
        if [ -n "$DURATION" ] && [ "$DURATION" != "null" ]; then
          COMMAND="$COMMAND --duration=$DURATION"
          echo "⏱️ Using duration: ${DURATION}s"
        fi
        
        echo "========================================"
        echo "🚀 Executing: $COMMAND"
        echo "========================================"
        
        # Execute the command
        $COMMAND
        
        # Save video info if created
        if [ -d "extracted_date_pages" ]; then
          VIDEO_FILE=$(ls extracted_date_pages/*.mp4 2>/dev/null | head -1)
          if [ -n "$VIDEO_FILE" ]; then
            echo "video_file=$VIDEO_FILE" >> $GITHUB_OUTPUT
            echo "✅ Video created: $VIDEO_FILE"
          fi
        fi
      env:
        PYTHONUNBUFFERED: 1
    
    - name: Upload video as artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: creative-daily-video-${{ github.run_id }}
        path: extracted_date_pages/*.mp4
        retention-days: 30
    
    - name: Upload images as artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: extracted-images-${{ github.run_id }}
        path: extracted_date_pages/*.png
        retention-days: 30
    
    - name: Upload all outputs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: creative-daily-outputs-${{ github.run_id }}
        path: |
          extracted_date_pages/
          *.log
        retention-days: 14
    
    - name: Summary
      if: always()
      run: |
        echo "========================================"
        echo "📊 CREATIVE DAILY SUMMARY"
        echo "========================================"
        echo "Run ID: ${{ github.run_id }}"
        echo "Status: ${{ job.status }}"
        echo ""
        
        if [ -d "extracted_date_pages" ]; then
          echo "📁 extracted_date_pages/ contents:"
          ls -la extracted_date_pages/
          echo ""
          
          VIDEO_COUNT=$(ls extracted_date_pages/*.mp4 2>/dev/null | wc -l)
          IMAGE_COUNT=$(ls extracted_date_pages/*.png 2>/dev/null | wc -l)
          
          echo "✅ Video files: $VIDEO_COUNT"
          echo "✅ Image files: $IMAGE_COUNT"
          
          # Show YouTube URL if uploaded
          if [ -f "extracted_date_pages/youtube_url.txt" ]; then
            echo ""
            echo "📹 YouTube URL: $(cat extracted_date_pages/youtube_url.txt)"
          fi
        else
          echo "❌ No extracted_date_pages directory found"
        fi
        echo "========================================"te_pages/ contents:"
          ls -la extracted_date_pages/
          echo ""
          
          VIDEO_COUNT=$(ls extracted_date_pages/*.mp4 2>/dev/null | wc -l)
          IMAGE_COUNT=$(ls extracted_date_pages/*.png 2>/dev/null | wc -l)
          
          echo "✅ Video files: $VIDEO_COUNT"
          echo "✅ Image files: $IMAGE_COUNT"
          
          # Show YouTube URL if uploaded
          if [ -f "extracted_date_pages/youtube_url.txt" ]; then
            echo ""
            echo "📹 YouTube URL: $(cat extracted_date_pages/youtube_url.txt)"
          fi
        else
          echo "❌ No extracted_date_pages directory found"
        fi
        echo "========================================"
